//começa quando o documento carregar
document.addEventListener("DOMContentLoaded",() =>{
    botao = document.getElementById("modalidade")
    botao.addEventListener("click",()=>{
        formulario = document.getElementById("evento")
        formulario
    })
})